import React from "react";
import { useSelector } from "react-redux";

import {  } from 'ReduxStore/selectors'

export const useGetEpisodesList = () => {
  const episodes = useSelector() || {};

  

  return {
    
  }
}